.venv/
__pycache__/
*.pyc
.pytest_cache/
.env
.claude/settings.local.json
uv.lock
runs/
*.wav
*.mp4

# VOX-029. Internal HR policy PDFs and the text extracted from them. The corpus is internal
# company documentation, so neither the PDFs nor their chunks belong in a repo other people
# clone; `make index` rebuilds the chunk file from whatever is in the folder. runs/ is already
# ignored above, which is why the chunk file is written there.
sources/

# The web coach's chat file and the NotebookLM Video Overviews it embeds: per-machine session
# state and large media, regenerated by `make coach` and the sync script.
docs/learning/coach/chat.jsonl
docs/learning/coach/videos/
